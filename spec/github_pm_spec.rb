require 'spec_helper'

describe GithubPm do
  it 'should have a version number' do
    expect(GithubPm::VERSION).not_to be nil
  end

  it 'should do something useful' do
    expect(false).to eq(true)
  end
end
